# Shared across all version/loader nodes.
# Base version; each node publishes as ${mod_version}+${minecraft} (e.g. 1.0.1+26.2).
mod_version=1.0.1

org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.configuration-cache=false
